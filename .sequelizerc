const path = require('path');

module.exports = {
  config: path.resolve('src/config', 'database.ts'), // ou config.json
  'models-path': path.resolve('src/models'),
  'seeders-path': path.resolve('src/scripts'),
};
